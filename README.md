# dotfiles

## Installation

1. Install chezmoi `brew install chezmoi`
2. Install xcode-commandline-tools `xcode-select --install`
3. Generate Github access token for new machine at https://github.com/settings/tokens
4. Initialize everything through chezmoi `chezmoi init https://github.com/pkaramishev/dotfiles.git` (use token as a password)